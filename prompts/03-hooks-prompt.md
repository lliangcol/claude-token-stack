# 03 Hooks Prompt

Implement and harden warn-first Claude Code hooks for high-noise command detection and code discovery guidance.

Required defaults: warn mode, cross-platform runner, explicit block opt-in, and clear fallback behavior.
