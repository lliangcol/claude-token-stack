# 00 Master Build Prompt

Build `claude-token-stack` in phases. Keep defaults conservative: warn first, no default block mode, no default Headroom, no remote install unless explicitly enabled, and no claim that total bills drop 90%.

Use repository evidence before editing. Keep each phase independently reviewable and validate after changes.
