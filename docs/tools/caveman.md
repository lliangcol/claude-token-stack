# Caveman

Caveman is treated as an optional output-style integration. This project does not install it automatically.

Use the included `templates/.claude/output-styles/token-lean.md` as the static fallback.
